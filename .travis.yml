language: objective-c

before_install: 
- brew update
- if brew outdated | grep -qx xctool; then brew upgrade xctool; fi

script: 
- xctool clean build -project ObjcSorts.xcodeproj -scheme ObjcSortsRelease
- xctool test -project ObjcSorts.xcodeproj -scheme ObjcSortsTests
